# 🚀 Быстрый старт

Пошаговая инструкция для запуска бота за 10 минут.

## Шаг 1: Установка зависимостей

```bash
npm install
```

## Шаг 2: Создание Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите имя бота (например: "Swimming Coach Bot")
4. Введите username бота (например: "my_swimming_coach_bot")
5. Скопируйте полученный **Bot Token**

## Шаг 3: Получение вашего Telegram ID

1. Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте любое сообщение
3. Скопируйте ваш **ID** (например: 123456789)

## Шаг 4: Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Откройте `.env` и заполните:

```env
BOT_TOKEN=ваш_токен_от_botfather
MONGODB_URI=mongodb://localhost:27017/swimming-coach-bot
ADMIN_IDS=ваш_telegram_id
TZ=Europe/Moscow
```

## Шаг 5: Запуск MongoDB

### Вариант A: Через Docker (рекомендуется)
```bash
docker run -d -p 27017:27017 --name mongodb mongo:latest
```

### Вариант B: Локальная установка

**macOS (Homebrew):**
```bash
brew tap mongodb/brew
brew install mongodb-community
brew services start mongodb-community
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt-get install mongodb
sudo systemctl start mongodb
```

**Windows:**
Скачайте и установите с [официального сайта](https://www.mongodb.com/try/download/community)

## Шаг 6: Инициализация базы данных

Создайте начальные временные слоты:

```bash
npm run init-db
```

Вы увидите:
```
✅ Подключено к MongoDB
✅ Создан слот: Пн 09:00-10:00
✅ Создан слот: Пн 10:00-11:00
...
✅ База данных инициализирована!
```

## Шаг 7: Запуск бота

```bash
npm run dev
```

Вы увидите:
```
MongoDB подключена: localhost
✅ Сервис напоминаний запущен
🤖 Бот запущен!
```

## Шаг 8: Тестирование

1. Откройте Telegram
2. Найдите вашего бота по username
3. Отправьте `/start`
4. Попробуйте записаться на тренировку!

---

## 🎉 Готово!

Ваш бот работает! Теперь вы можете:

- ✅ Записываться на тренировки
- ✅ Просматривать расписание
- ✅ Управлять записями
- ✅ Использовать админ-панель (если вы админ)

---

## 🔧 Настройка расписания

Если хотите изменить расписание, отредактируйте `scripts/initDatabase.ts` и запустите снова:

```bash
npm run init-db
```

Или добавьте слоты вручную через MongoDB:

```javascript
// Подключитесь к MongoDB
mongosh

// Выберите базу данных
use swimming-coach-bot

// Добавьте новый слот
db.timeslots.insertOne({
  dayOfWeek: 2,  // 0=Вс, 1=Пн, 2=Вт, 3=Ср, 4=Чт, 5=Пт, 6=Сб
  startTime: "14:00",
  endTime: "15:00",
  isActive: true,
  maxClients: 1
})
```

---

## 🐛 Решение проблем

### Бот не отвечает
- Проверьте, что BOT_TOKEN правильный
- Убедитесь, что бот запущен (`npm run dev`)
- Проверьте логи в консоли

### Ошибка подключения к MongoDB
- Убедитесь, что MongoDB запущена
- Проверьте MONGODB_URI в .env
- Попробуйте: `mongosh` для проверки подключения

### Нет доступа к админ-панели
- Проверьте, что ваш ID в ADMIN_IDS
- Перезапустите бота
- Отправьте `/start` заново

### Не приходят напоминания
- Напоминания отправляются за 1 час до тренировки
- Проверяются каждые 5 минут
- Создайте тестовую тренировку на время через 1 час

---

## 📚 Дополнительная информация

- [README.md](README.md) - Полная документация
- [DEVELOPMENT_PLAN.md](DEVELOPMENT_PLAN.md) - План развития
- [Telegraf документация](https://telegraf.js.org/)
- [MongoDB документация](https://www.mongodb.com/docs/)

---

## 💬 Нужна помощь?

Если что-то не работает, проверьте:
1. Все ли зависимости установлены (`npm install`)
2. Правильно ли заполнен `.env`
3. Запущена ли MongoDB
4. Нет ли ошибок в консоли

Удачи! 🏊‍♂️
