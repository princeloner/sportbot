# 🎯 Инструкция по настройке бота

## ✅ Что уже сделано

1. ✅ Базовый функционал бота (запись, отмена, расписание)
2. ✅ Админ-панель со статистикой
3. ✅ Автоматические напоминания
4. ✅ **НОВОЕ:** Обучающие материалы по стилям плавания
5. ✅ Полная документация

## 🚀 Что нужно сделать сейчас

### 1. Исправить базу данных

```bash
npm run fix-db
```

Это удалит проблемный индекс и исправит подключение к базе `sportpoolbot`.

### 2. Перезапустить бота

```bash
npm run dev
```

### 3. Проверить работу

Откройте бота в Telegram и отправьте `/start`

Теперь должно работать без ошибок!

### 4. Добавить обучающие материалы с медиа

```bash
npm run add-media
```

Это автоматически найдет все файлы в папке `media/` и добавит их в базу данных!

### 5. Проверить обучение

В боте нажмите **"📚 Обучение"** → выберите стиль → получите материалы!

---

## 📸 Добавление фото и голосовых

### Способ 1: Через Telegram File ID (Рекомендуется)

1. **Добавьте временный код** в `src/index.ts` после строки `bot.command('start', handleStart);`:

```typescript
// Временно - для получения File ID
bot.on('photo', async (ctx) => {
  const fileId = ctx.message.photo[ctx.message.photo.length - 1].file_id;
  await ctx.reply(`📸 Photo File ID:\n\`${fileId}\`\n\nСкопируйте и используйте в MongoDB`);
});

bot.on('voice', async (ctx) => {
  const fileId = ctx.message.voice.file_id;
  await ctx.reply(`🎤 Voice File ID:\n\`${fileId}\`\n\nСкопируйте и используйте в MongoDB`);
});

bot.on('video', async (ctx) => {
  const fileId = ctx.message.video.file_id;
  await ctx.reply(`🎥 Video File ID:\n\`${fileId}\`\n\nСкопируйте и используйте в MongoDB`);
});
```

2. **Перезапустите бота:**
```bash
npm run dev
```

3. **Отправьте файлы боту:**
   - Отправьте фото с демонстрацией техники
   - Запишите и отправьте голосовое объяснение
   - Отправьте видео (если есть)

4. **Скопируйте File ID** из ответа бота

5. **Обновите материал в MongoDB:**

```bash
mongosh sportpoolbot
```

```javascript
// Пример для баттерфляя
db.tutorials.updateOne(
  { 
    style: "butterfly", 
    title: "Техника баттерфляя для начинающих" 
  },
  { 
    $set: { 
      photoUrl: "AgACAgIAAxkBAAIC...",  // Вставьте File ID фото
      voiceUrl: "AwACAgIAAxkBAAIC..."   // Вставьте File ID голосового
    } 
  }
)
```

6. **Проверьте** - откройте бота, нажмите "📚 Обучение" → "🦋 Баттерфляй"

7. **Удалите временный код** из `src/index.ts` после добавления всех файлов

---

## 📋 Чек-лист настройки

- [ ] Запустить `npm run fix-db`
- [ ] Перезапустить бота `npm run dev`
- [ ] Проверить работу `/start`
- [ ] Запустить `npm run add-tutorials`
- [ ] Проверить кнопку "📚 Обучение"
- [ ] Добавить временный код для File ID
- [ ] Отправить фото/голосовые боту
- [ ] Скопировать File ID
- [ ] Обновить материалы в MongoDB
- [ ] Проверить отправку медиа
- [ ] Удалить временный код

---

## 🎨 Рекомендации по контенту

### Фото:
- Четкие снимки техники плавания
- Разные углы (сбоку, сверху)
- Хорошее освещение
- Формат: JPG, PNG
- Размер: до 10MB

### Голосовые:
- Длительность: 30-60 секунд
- Четкая речь
- Объяснение ключевых моментов
- Без фонового шума
- Формат: OGG, MP3

### Видео:
- Демонстрация упражнения
- Длительность: 30-90 секунд
- Качество: 720p или выше
- Формат: MP4
- Размер: до 50MB

---

## 📚 Структура материалов

Рекомендуемая структура для каждого стиля:

1. **Основы техники** (order: 1)
   - Фото + голосовое объяснение
   - Ключевые моменты

2. **Упражнения** (order: 2, 3, 4...)
   - Фото упражнения
   - Голосовое с инструкцией
   - Видео демонстрация (опционально)

3. **Частые ошибки** (order: 10)
   - Текст с описанием
   - Фото правильно/неправильно

4. **Советы** (order: 11+)
   - Текстовые советы
   - Дополнительные материалы

---

## 🔧 Полезные команды MongoDB

### Посмотреть все материалы:
```javascript
db.tutorials.find().pretty()
```

### Материалы по стилю:
```javascript
db.tutorials.find({ style: "butterfly" }).sort({ order: 1 })
```

### Обновить описание:
```javascript
db.tutorials.updateOne(
  { title: "Название" },
  { $set: { description: "Новое описание" } }
)
```

### Деактивировать:
```javascript
db.tutorials.updateOne(
  { title: "Название" },
  { $set: { isActive: false } }
)
```

### Удалить:
```javascript
db.tutorials.deleteOne({ title: "Название" })
```

---

## 📖 Дополнительная документация

- **TUTORIALS_GUIDE.md** - полное руководство по обучающим материалам
- **TUTORIALS_QUICKSTART.md** - быстрый старт
- **README.md** - основная документация проекта
- **FAQ.md** - часто задаваемые вопросы

---

## 💬 Нужна помощь?

Если что-то не работает:
1. Проверьте логи в консоли
2. Убедитесь, что MongoDB запущена
3. Проверьте .env файл
4. Посмотрите FAQ.md

Удачи! 🚀
