# 📚 Руководство по обучающим материалам

## 🎯 Что это?

Функция "Обучение" позволяет отправлять клиентам обучающие материалы по разным стилям плавания:
- 🦋 Баттерфляй
- 🏊 Вольный стиль (кроль)
- 🔙 На спине
- 🐸 Брасс
- 📖 Общие советы

## 📁 Структура материалов

Каждый материал может содержать:
- ✅ **Текст** - описание техники
- ✅ **Фото** - демонстрация упражнения
- ✅ **Голосовое** - объяснение тренера
- ✅ **Видео** - видео-демонстрация

## 🚀 Быстрый старт

### 1. Добавить примерные материалы

```bash
npm run add-tutorials
```

Это добавит базовые текстовые материалы по всем стилям.

### 2. Проверить в боте

1. Откройте бота
2. Нажмите "📚 Обучение"
3. Выберите стиль плавания
4. Получите материалы

## 📸 Добавление медиа-файлов

### Вариант 1: Локальные файлы

1. **Создайте папку для медиа:**
```bash
mkdir -p media/photos
mkdir -p media/voices
mkdir -p media/videos
```

2. **Добавьте файлы:**
```
media/
├── photos/
│   ├── butterfly-technique.jpg
│   ├── freestyle-breathing.jpg
│   └── ...
├── voices/
│   ├── butterfly-explanation.ogg
│   └── ...
└── videos/
    ├── butterfly-demo.mp4
    └── ...
```

3. **Обновите материал в MongoDB:**
```javascript
mongosh sportpoolbot

db.tutorials.updateOne(
  { style: "butterfly", title: "Техника баттерфляя для начинающих" },
  { 
    $set: { 
      photoPath: "./media/photos/butterfly-technique.jpg",
      voicePath: "./media/voices/butterfly-explanation.ogg"
    } 
  }
)
```

### Вариант 2: URL ссылки

Если файлы хранятся в облаке (Telegram, Google Drive, etc):

```javascript
db.tutorials.updateOne(
  { style: "butterfly", title: "Техника баттерфляя для начинающих" },
  { 
    $set: { 
      photoUrl: "https://example.com/photo.jpg",
      voiceUrl: "https://example.com/voice.ogg",
      videoUrl: "https://example.com/video.mp4"
    } 
  }
)
```

### Вариант 3: Telegram File ID (Рекомендуется!)

Самый простой способ - загрузить файлы в Telegram и использовать их File ID:

1. **Отправьте файл боту** (фото/голосовое/видео)

2. **Получите File ID** - добавьте временный обработчик:
```typescript
// В src/index.ts временно добавьте:
bot.on('photo', async (ctx) => {
  const fileId = ctx.message.photo[ctx.message.photo.length - 1].file_id;
  await ctx.reply(`Photo File ID: ${fileId}`);
});

bot.on('voice', async (ctx) => {
  const fileId = ctx.message.voice.file_id;
  await ctx.reply(`Voice File ID: ${fileId}`);
});

bot.on('video', async (ctx) => {
  const fileId = ctx.message.video.file_id;
  await ctx.reply(`Video File ID: ${fileId}`);
});
```

3. **Используйте File ID:**
```javascript
db.tutorials.updateOne(
  { style: "butterfly", title: "Техника баттерфляя для начинающих" },
  { 
    $set: { 
      photoUrl: "AgACAgIAAxkBAAIC...",  // File ID
      voiceUrl: "AwACAgIAAxkBAAIC..."   // File ID
    } 
  }
)
```

## 📝 Создание нового материала

### Через MongoDB:

```javascript
mongosh sportpoolbot

db.tutorials.insertOne({
  style: "butterfly",
  title: "Упражнение: Волна на груди",
  description: "Упражнение для отработки волнообразного движения:\n\n1. Возьмите доску\n2. Работайте только ногами\n3. Делайте волну всем телом\n4. 4 подхода по 25 метров",
  photoUrl: "AgACAgIAAxkBAAIC...",
  voiceUrl: "AwACAgIAAxkBAAIC...",
  order: 3,
  isActive: true
})
```

### Через скрипт:

Создайте файл `scripts/addCustomTutorial.ts`:

```typescript
import dotenv from 'dotenv';
dotenv.config();

import mongoose from 'mongoose';
import Tutorial from '../src/models/Tutorial';

const addCustomTutorial = async () => {
  await mongoose.connect(process.env.MONGODB_URI!);

  await Tutorial.create({
    style: 'butterfly',
    title: 'Ваш заголовок',
    description: 'Ваше описание',
    photoUrl: 'File ID или URL',
    voiceUrl: 'File ID или URL',
    order: 1,
    isActive: true
  });

  console.log('✅ Материал добавлен!');
  await mongoose.disconnect();
};

addCustomTutorial();
```

## 🎨 Форматы файлов

### Фото:
- JPG, PNG
- Рекомендуемый размер: 1280x720px
- Максимум: 10MB

### Голосовые:
- OGG (Telegram формат)
- MP3 (конвертируется автоматически)
- Максимум: 20MB

### Видео:
- MP4, MOV
- Рекомендуемое разрешение: 720p или 1080p
- Максимум: 50MB

## 🔧 Управление материалами

### Просмотр всех материалов:
```javascript
db.tutorials.find().pretty()
```

### Материалы по стилю:
```javascript
db.tutorials.find({ style: "butterfly" }).sort({ order: 1 })
```

### Деактивировать материал:
```javascript
db.tutorials.updateOne(
  { _id: ObjectId("...") },
  { $set: { isActive: false } }
)
```

### Изменить порядок:
```javascript
db.tutorials.updateOne(
  { _id: ObjectId("...") },
  { $set: { order: 5 } }
)
```

### Удалить материал:
```javascript
db.tutorials.deleteOne({ _id: ObjectId("...") })
```

## 💡 Советы

### 1. Качество контента
- Используйте четкие фото
- Записывайте голосовые в тихом месте
- Снимайте видео в хорошем освещении

### 2. Структура материалов
- Начинайте с основ (order: 1)
- Затем упражнения (order: 2, 3...)
- Заканчивайте советами (order: 10+)

### 3. Описания
- Используйте нумерованные списки
- Добавляйте эмодзи для наглядности
- Пишите кратко и понятно

### 4. Голосовые объяснения
- Длительность: 30-60 секунд
- Говорите четко и медленно
- Объясняйте ключевые моменты

## 🎯 Примеры использования

### Пример 1: Полный материал с медиа

```javascript
{
  style: "butterfly",
  title: "Техника гребка руками",
  description: "Правильный гребок:\n\n1. Руки входят вместе\n2. Гребок в стороны\n3. Выход у бедер\n4. Пронос над водой\n\nСмотрите фото и слушайте объяснение!",
  photoUrl: "AgACAgIAAxkBAAIC...",
  voiceUrl: "AwACAgIAAxkBAAIC...",
  videoUrl: "BAACAgIAAxkBAAIC...",
  order: 2,
  isActive: true
}
```

### Пример 2: Только текст

```javascript
{
  style: "general",
  title: "Питание пловца",
  description: "Рекомендации по питанию:\n\n🥗 До тренировки (за 2 часа):\n• Углеводы (каша, банан)\n• Немного белка\n\n💧 Во время:\n• Пейте воду\n\n🍗 После (в течение часа):\n• Белок + углеводы\n• Восстановление мышц",
  order: 5,
  isActive: true
}
```

## 🚀 Расширенные возможности

### Добавить админ-панель для управления

В будущем можно добавить:
- Загрузку файлов через бота
- Редактирование материалов
- Предпросмотр перед публикацией
- Статистику просмотров

### Персонализация

Можно добавить:
- Материалы для разных уровней (начинающий, средний, продвинутый)
- Рекомендации на основе прогресса клиента
- Персональные программы тренировок

## 📊 Статистика

Отслеживайте популярность материалов:

```javascript
// Добавьте поле views в модель
db.tutorials.updateOne(
  { _id: ObjectId("...") },
  { $inc: { views: 1 } }
)

// Самые популярные материалы
db.tutorials.find().sort({ views: -1 }).limit(10)
```

## ❓ FAQ

**Q: Можно ли отправлять несколько фото?**
A: Да, создайте несколько материалов с одинаковым стилем и разными order.

**Q: Как записать голосовое?**
A: Запишите в Telegram и отправьте боту, получите File ID.

**Q: Где хранить большие видео?**
A: YouTube, Google Drive, или Telegram (до 50MB).

**Q: Можно ли добавить PDF?**
A: Да, используйте `ctx.replyWithDocument()`.

---

Готово! Теперь у вас есть полноценная система обучающих материалов! 🎉
