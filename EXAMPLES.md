# 📖 Примеры использования

## Сценарии использования бота

### 1️⃣ Сценарий: Новый клиент записывается на первую тренировку

**Клиент:**
1. Находит бота в Telegram
2. Отправляет `/start`
3. Получает приветственное сообщение
4. Нажимает "📅 Записаться на тренировку"
5. Выбирает день (например, Среда)
6. Выбирает время (например, 10:00-11:00)
7. Получает подтверждение

**Результат:**
```
✅ Отлично! Вы записаны на тренировку:

📅 03 января 2026, 10:00

⏰ Вы получите напоминание за час до тренировки!
```

---

### 2️⃣ Сценарий: Клиент проверяет свои тренировки

**Клиент:**
1. Нажимает "📋 Мои тренировки"
2. Видит список предстоящих тренировок

**Результат:**
```
📋 Ваши предстоящие тренировки:

1. 📅 03 января 2026, 10:00
2. 📅 05 января 2026, 18:00
3. 📅 08 января 2026, 09:00
```

---

### 3️⃣ Сценарий: Клиент отменяет тренировку

**Клиент:**
1. Нажимает "❌ Отменить запись"
2. Выбирает тренировку из списка
3. Подтверждает отмену

**Результат:**
```
✅ Тренировка на 03 января 2026, 10:00 успешно отменена.
```

---

### 4️⃣ Сценарий: Клиент получает напоминание

**Система (за 1 час до тренировки):**
```
⏰ Напоминание!

🏊‍♂️ Через час у вас тренировка!

📅 03 января 2026, 10:00

Не забудьте взять с собой все необходимое! 💪
```

---

### 5️⃣ Сценарий: Тренер проверяет статистику

**Тренер:**
1. Нажимает "👨‍💼 Админ-панель"
2. Нажимает "📊 Статистика"

**Результат:**
```
📊 Статистика:

👥 Всего клиентов: 15
📅 Запланировано тренировок: 23

📈 За эту неделю:
   ✅ Проведено: 12

📈 За этот месяц:
   ✅ Проведено: 45
   ❌ Отменено: 3
```

---

### 6️⃣ Сценарий: Тренер просматривает список клиентов

**Тренер:**
1. Нажимает "👨‍💼 Админ-панель"
2. Нажимает "👥 Список клиентов"

**Результат:**
```
👥 Список клиентов:

👤 Иван Петров
   @ivan_petrov
   📊 Тренировок: 24

👤 Мария Сидорова
   @maria_sid
   📊 Тренировок: 18

👤 Алексей Иванов
   @alex_ivanov
   📊 Тренировок: 12
```

---

### 7️⃣ Сценарий: Тренер просматривает предстоящие тренировки

**Тренер:**
1. Нажимает "👨‍💼 Админ-панель"
2. Нажимает "📅 Все тренировки"

**Результат:**
```
📅 Предстоящие тренировки:

1. 03 января 2026, 09:00
   👤 Иван Петров
   @ivan_petrov

2. 03 января 2026, 10:00
   👤 Мария Сидорова
   @maria_sid

3. 03 января 2026, 18:00
   👤 Алексей Иванов
   @alex_ivanov
```

---

## 🔧 Примеры работы с базой данных

### Добавление нового временного слота

```javascript
// Через MongoDB shell
use swimming-coach-bot

db.timeslots.insertOne({
  dayOfWeek: 2,        // Вторник
  startTime: "14:00",
  endTime: "15:00",
  isActive: true,
  maxClients: 1
})
```

### Деактивация слота

```javascript
db.timeslots.updateOne(
  { dayOfWeek: 2, startTime: "14:00" },
  { $set: { isActive: false } }
)
```

### Просмотр всех тренировок клиента

```javascript
// Сначала найдите ID клиента
db.users.findOne({ telegramId: 123456789 })

// Затем найдите его тренировки
db.trainings.find({ 
  clientId: ObjectId("..."),
  status: "scheduled"
}).sort({ date: 1 })
```

### Отметка тренировки как проведенной

```javascript
db.trainings.updateOne(
  { _id: ObjectId("...") },
  { $set: { status: "completed" } }
)
```

### Статистика по клиенту

```javascript
// Количество проведенных тренировок
db.trainings.countDocuments({
  clientId: ObjectId("..."),
  status: "completed"
})

// Количество отмененных
db.trainings.countDocuments({
  clientId: ObjectId("..."),
  status: "cancelled"
})
```

---

## 🎯 Примеры расширения функционала

### Добавление заметок к тренировке

```typescript
// В модели Training добавьте:
notes?: string;

// В админ-панели добавьте обработчик:
bot.action(/^add_note_/, async (ctx) => {
  const trainingId = ctx.callbackQuery.data.split('_')[2];
  // Запросить заметку у тренера
  // Сохранить в базу
});
```

### Система абонементов

```typescript
// Новая модель Subscription
interface ISubscription {
  clientId: ObjectId;
  totalTrainings: number;
  remainingTrainings: number;
  expiresAt: Date;
  isActive: boolean;
}

// При записи на тренировку списывать из абонемента
if (subscription && subscription.remainingTrainings > 0) {
  subscription.remainingTrainings--;
  await subscription.save();
}
```

### Групповые тренировки

```typescript
// В TimeSlot увеличьте maxClients:
maxClients: 5  // Вместо 1

// При записи проверяйте количество:
const count = await Training.countDocuments({
  date: slotDate,
  status: 'scheduled'
});

if (count < slot.maxClients) {
  // Разрешить запись
}
```

---

## 📱 Примеры интеграций

### Интеграция с Google Calendar

```typescript
import { google } from 'googleapis';

const calendar = google.calendar('v3');

// При создании тренировки
const event = {
  summary: `Тренировка с ${client.firstName}`,
  start: { dateTime: training.date.toISOString() },
  end: { dateTime: addHours(training.date, 1).toISOString() },
};

await calendar.events.insert({
  calendarId: 'primary',
  resource: event,
});
```

### Интеграция с платежами (ЮKassa)

```typescript
import { YooCheckout } from '@a2seven/yoo-checkout';

const checkout = new YooCheckout({
  shopId: process.env.YOOKASSA_SHOP_ID,
  secretKey: process.env.YOOKASSA_SECRET_KEY,
});

// Создание платежа
const payment = await checkout.createPayment({
  amount: { value: '1000.00', currency: 'RUB' },
  description: 'Оплата тренировки',
  confirmation: { type: 'redirect', return_url: '...' },
});
```

---

## 🧪 Примеры тестирования

### Тест регистрации пользователя

```typescript
import { Telegraf } from 'telegraf';
import { handleStart } from './handlers/start';

describe('User Registration', () => {
  it('should register new user', async () => {
    const ctx = mockContext({ id: 123, first_name: 'Test' });
    await handleStart(ctx);
    
    const user = await User.findOne({ telegramId: 123 });
    expect(user).toBeDefined();
    expect(user.firstName).toBe('Test');
  });
});
```

### Тест записи на тренировку

```typescript
describe('Booking', () => {
  it('should create training', async () => {
    const user = await User.create({ telegramId: 123, firstName: 'Test' });
    const date = addDays(new Date(), 1);
    
    const training = await Training.create({
      clientId: user._id,
      date,
      status: 'scheduled'
    });
    
    expect(training.status).toBe('scheduled');
  });
});
```

---

## 🚀 Примеры деплоя

### Деплой на VPS (Ubuntu)

```bash
# Подключитесь к серверу
ssh user@your-server.com

# Установите Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# Установите MongoDB
wget -qO - https://www.mongodb.org/static/pgp/server-6.0.asc | sudo apt-key add -
sudo apt-get install -y mongodb-org

# Клонируйте репозиторий
git clone <your-repo>
cd swimming-coach-bot

# Установите зависимости
npm install

# Настройте .env
nano .env

# Соберите проект
npm run build

# Установите PM2
npm install -g pm2

# Запустите бота
pm2 start dist/index.js --name swimming-bot
pm2 save
pm2 startup
```

### Деплой через Docker

```bash
# Соберите образ
docker build -t swimming-coach-bot .

# Запустите контейнеры
docker-compose up -d

# Проверьте логи
docker-compose logs -f bot
```

---

## 💡 Полезные команды

### Мониторинг

```bash
# Логи бота (PM2)
pm2 logs swimming-bot

# Статус
pm2 status

# Перезапуск
pm2 restart swimming-bot

# Логи Docker
docker-compose logs -f
```

### Бэкап базы данных

```bash
# Создать бэкап
mongodump --db swimming-coach-bot --out ./backup

# Восстановить бэкап
mongorestore --db swimming-coach-bot ./backup/swimming-coach-bot
```

### Очистка данных

```bash
# Удалить старые тренировки (старше 3 месяцев)
mongosh swimming-coach-bot --eval "
  db.trainings.deleteMany({
    date: { \$lt: new Date(Date.now() - 90*24*60*60*1000) },
    status: { \$in: ['completed', 'cancelled', 'missed'] }
  })
"
```

---

Эти примеры помогут вам лучше понять, как работает бот и как его можно расширить! 🎉
