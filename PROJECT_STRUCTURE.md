# 📁 Структура проекта

```
swimming-coach-bot/
│
├── 📂 src/                          # Исходный код приложения
│   ├── 📂 config/                   # Конфигурация
│   │   └── database.ts              # Подключение к MongoDB
│   │
│   ├── 📂 models/                   # Модели данных (Mongoose)
│   │   ├── User.ts                  # Модель пользователя
│   │   ├── Training.ts              # Модель тренировки
│   │   └── TimeSlot.ts              # Модель временного слота
│   │
│   ├── 📂 handlers/                 # Обработчики команд и действий
│   │   ├── start.ts                 # /start - регистрация
│   │   ├── booking.ts               # Запись на тренировку
│   │   ├── myTrainings.ts           # Просмотр своих тренировок
│   │   ├── schedule.ts              # Расписание тренера
│   │   ├── cancel.ts                # Отмена записи
│   │   └── admin.ts                 # Админ-панель
│   │
│   ├── 📂 services/                 # Сервисы и фоновые задачи
│   │   └── reminderService.ts       # Автоматические напоминания
│   │
│   ├── 📂 utils/                    # Вспомогательные функции
│   │   ├── keyboards.ts             # Клавиатуры бота
│   │   └── helpers.ts               # Утилиты (форматирование, проверки)
│   │
│   └── index.ts                     # Главный файл приложения
│
├── 📂 scripts/                      # Скрипты для управления
│   └── initDatabase.ts              # Инициализация базы данных
│
├── 📂 dist/                         # Скомпилированный код (создается при сборке)
│
├── 📄 .env.example                  # Пример переменных окружения
├── 📄 .env                          # Ваши переменные окружения (не в git)
├── 📄 .gitignore                    # Игнорируемые файлы
├── 📄 .dockerignore                 # Игнорируемые файлы для Docker
│
├── 📄 package.json                  # Зависимости и скрипты
├── 📄 package-lock.json             # Точные версии зависимостей
├── 📄 tsconfig.json                 # Конфигурация TypeScript
│
├── 📄 Dockerfile                    # Docker образ
├── 📄 docker-compose.yml            # Docker Compose конфигурация
│
├── 📄 README.md                     # Основная документация
├── 📄 QUICKSTART.md                 # Быстрый старт
├── 📄 DEVELOPMENT_PLAN.md           # План развития
├── 📄 EXAMPLES.md                   # Примеры использования
├── 📄 PROJECT_STRUCTURE.md          # Этот файл
└── 📄 LICENSE                       # Лицензия MIT
```

---

## 📋 Описание компонентов

### 🔧 Конфигурация

**config/database.ts**
- Подключение к MongoDB
- Обработка ошибок подключения
- Логирование статуса

### 📊 Модели данных

**models/User.ts**
- Хранение информации о пользователях
- Telegram ID, имя, username
- Флаг администратора
- Настройки уведомлений

**models/Training.ts**
- Записи на тренировки
- Связь с пользователем (clientId)
- Дата, время, длительность
- Статус (scheduled, completed, cancelled, missed)
- Флаг отправки напоминания

**models/TimeSlot.ts**
- Доступные временные слоты
- День недели (0-6)
- Время начала и конца
- Активность слота
- Максимальное количество клиентов

### 🎮 Обработчики

**handlers/start.ts**
- Команда /start
- Регистрация нового пользователя
- Приветственное сообщение
- Отображение клавиатуры

**handlers/booking.ts**
- Выбор даты тренировки
- Отображение доступных слотов
- Проверка занятости
- Создание записи

**handlers/myTrainings.ts**
- Список предстоящих тренировок
- Сортировка по дате
- Форматирование вывода

**handlers/schedule.ts**
- Расписание работы тренера
- Группировка по дням недели
- Отображение времени

**handlers/cancel.ts**
- Выбор тренировки для отмены
- Изменение статуса
- Подтверждение отмены

**handlers/admin.ts**
- Доступ к админ-панели
- Статистика (клиенты, тренировки)
- Список клиентов
- Управление графиком
- Просмотр всех тренировок

### 🔄 Сервисы

**services/reminderService.ts**
- Cron-задача (каждые 5 минут)
- Поиск тренировок через 1 час
- Отправка напоминаний
- Отметка об отправке

### 🛠 Утилиты

**utils/keyboards.ts**
- Клавиатуры для клиентов
- Клавиатуры для админов
- Inline-клавиатуры для выбора

**utils/helpers.ts**
- Форматирование даты и времени
- Проверка прав администратора
- Получение названий дней
- Расчет дат

### 🚀 Главный файл

**index.ts**
- Инициализация бота
- Регистрация обработчиков
- Запуск сервисов
- Graceful shutdown

---

## 🗄 База данных

### Коллекции MongoDB

**users**
```javascript
{
  _id: ObjectId,
  telegramId: Number,
  username: String,
  firstName: String,
  lastName: String,
  phone: String,
  isAdmin: Boolean,
  notificationsEnabled: Boolean,
  createdAt: Date
}
```

**trainings**
```javascript
{
  _id: ObjectId,
  clientId: ObjectId,  // ref: User
  date: Date,
  duration: Number,
  status: String,      // scheduled, completed, cancelled, missed
  notes: String,
  reminderSent: Boolean,
  createdAt: Date
}
```

**timeslots**
```javascript
{
  _id: ObjectId,
  dayOfWeek: Number,   // 0-6
  startTime: String,   // "09:00"
  endTime: String,     // "10:00"
  isActive: Boolean,
  maxClients: Number
}
```

---

## 🔄 Поток данных

### Запись на тренировку

```
Клиент → Кнопка "Записаться"
    ↓
handlers/booking.ts → Показать даты
    ↓
Клиент выбирает дату
    ↓
handlers/booking.ts → Запрос TimeSlot
    ↓
Показать доступные слоты
    ↓
Клиент выбирает время
    ↓
handlers/booking.ts → Создать Training
    ↓
Сохранить в MongoDB
    ↓
Подтверждение клиенту
```

### Отправка напоминания

```
Cron (каждые 5 минут)
    ↓
services/reminderService.ts
    ↓
Запрос Training (через 1 час)
    ↓
Для каждой тренировки:
    ↓
Получить User
    ↓
Отправить сообщение
    ↓
Отметить reminderSent = true
```

### Админ-статистика

```
Админ → Кнопка "Статистика"
    ↓
handlers/admin.ts
    ↓
Запросы к MongoDB:
  - User.countDocuments()
  - Training.countDocuments()
    ↓
Форматирование данных
    ↓
Отправка сообщения
```

---

## 📦 Зависимости

### Production
- **telegraf** - Telegram Bot Framework
- **mongoose** - MongoDB ODM
- **dotenv** - Переменные окружения
- **node-cron** - Планировщик задач
- **date-fns** - Работа с датами

### Development
- **typescript** - Типизация
- **ts-node-dev** - Разработка с hot-reload
- **@types/node** - Типы Node.js
- **@types/node-cron** - Типы для node-cron

---

## 🔐 Переменные окружения

```env
BOT_TOKEN          # Токен от @BotFather
MONGODB_URI        # Строка подключения к MongoDB
ADMIN_IDS          # ID администраторов (через запятую)
TZ                 # Часовой пояс
```

---

## 🚀 Команды npm

```bash
npm run dev        # Запуск в режиме разработки
npm run build      # Компиляция TypeScript
npm start          # Запуск production версии
npm run init-db    # Инициализация базы данных
```

---

## 🐳 Docker

**Dockerfile**
- Базовый образ: node:18-alpine
- Установка зависимостей
- Компиляция TypeScript
- Запуск приложения

**docker-compose.yml**
- Сервис MongoDB
- Сервис бота
- Volumes для данных
- Переменные окружения

---

## 📝 Документация

- **README.md** - Основная документация, установка, использование
- **QUICKSTART.md** - Быстрый старт за 10 минут
- **DEVELOPMENT_PLAN.md** - Детальный план развития, roadmap
- **EXAMPLES.md** - Примеры использования, сценарии, код
- **PROJECT_STRUCTURE.md** - Структура проекта (этот файл)

---

## 🎯 Точки расширения

### Добавление новых функций

1. **Новая команда**
   - Создать handler в `handlers/`
   - Зарегистрировать в `index.ts`
   - Добавить кнопку в `utils/keyboards.ts`

2. **Новая модель**
   - Создать схему в `models/`
   - Добавить индексы
   - Использовать в handlers

3. **Новый сервис**
   - Создать файл в `services/`
   - Запустить в `index.ts`
   - Настроить cron если нужно

4. **Новая утилита**
   - Добавить функцию в `utils/helpers.ts`
   - Экспортировать
   - Использовать в handlers

---

Эта структура обеспечивает:
- ✅ Модульность
- ✅ Масштабируемость
- ✅ Читаемость
- ✅ Легкость поддержки
- ✅ Простоту тестирования
