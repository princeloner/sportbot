# 🚀 Быстрый старт: Обучающие материалы

## Шаг 1: Добавьте базовые материалы

```bash
npm run add-tutorials
```

Это добавит текстовые материалы по всем стилям плавания.

## Шаг 2: Проверьте в боте

1. Откройте бота в Telegram
2. Нажмите кнопку **"📚 Обучение"**
3. Выберите стиль (например, "🦋 Баттерфляй")
4. Получите материалы!

## Шаг 3: Добавьте медиа-файлы

### Самый простой способ - через Telegram File ID:

1. **Отправьте фото боту** (например, демонстрацию техники)

2. **Временно добавьте обработчик** в `src/index.ts`:
```typescript
// После строки bot.command('start', handleStart);
bot.on('photo', async (ctx) => {
  const fileId = ctx.message.photo[ctx.message.photo.length - 1].file_id;
  await ctx.reply(`📸 File ID:\n\`${fileId}\``);
});

bot.on('voice', async (ctx) => {
  const fileId = ctx.message.voice.file_id;
  await ctx.reply(`🎤 File ID:\n\`${fileId}\``);
});
```

3. **Перезапустите бота:**
```bash
npm run dev
```

4. **Отправьте файлы боту** и скопируйте File ID

5. **Обновите материал в MongoDB:**
```bash
mongosh sportpoolbot
```

```javascript
db.tutorials.updateOne(
  { 
    style: "butterfly", 
    title: "Техника баттерфляя для начинающих" 
  },
  { 
    $set: { 
      photoUrl: "AgACAgIAAxkBAAIC...",  // Вставьте ваш File ID
      voiceUrl: "AwACAgIAAxkBAAIC..."   // Вставьте ваш File ID
    } 
  }
)
```

6. **Проверьте результат** - откройте бота и выберите материал!

## Готово! 🎉

Теперь клиенты получат:
- ✅ Текстовое описание
- ✅ Фото с демонстрацией
- ✅ Голосовое объяснение

## Дополнительно

### Добавить новый материал:

```javascript
db.tutorials.insertOne({
  style: "butterfly",
  title: "Упражнение: Работа ног",
  description: "Упражнение для отработки ударов ногами:\n\n1. Возьмите доску\n2. Работайте только ногами\n3. Делайте волну от груди\n4. 4x25 метров",
  photoUrl: "AgACAgIAAxkBAAIC...",
  order: 3,
  isActive: true
})
```

### Посмотреть все материалы:

```javascript
db.tutorials.find().pretty()
```

### Деактивировать материал:

```javascript
db.tutorials.updateOne(
  { title: "Название материала" },
  { $set: { isActive: false } }
)
```

---

Подробная документация: [TUTORIALS_GUIDE.md](TUTORIALS_GUIDE.md)
